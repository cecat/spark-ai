# secrets.yaml.example — copy to secrets.yaml and fill in real values
#
# secrets.yaml is gitignored. Never commit it.
#
# To find your Tailscale IP:  tailscale ip -4
# To find your workspace dir: look at openclaw/docker-compose.yml → OPENCLAW_WORKSPACE

anthropic_api_key: REPLACE_ME   # sk-ant-...
